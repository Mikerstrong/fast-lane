services:
  fast-lane:
    container_name: fast-lane
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    environment:
      - FASTLANE_DATA_DIR=/data
    volumes:
      - fastlane_data:/data
    restart: unless-stopped

volumes:
  fastlane_data:
